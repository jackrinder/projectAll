<% provide(:title, "All users") %>
<h1>All users</h1>

<!-- will_paginate shows the page links above the list -->
<%= will_paginate %>
<ul class="users">
<!-- see page 329 for info about the syntax -->
		<%= render @users %>
	
</ul>

<!-- shows the page links below the list -->
<%= will_paginate %>
